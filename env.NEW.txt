# ---- server ----
NODE_ENV=development
PORT=5000

# ---- database ----
# Local:  mongodb://127.0.0.1:27017/mobile-shop
# Atlas:  mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/mobile?retryWrites=true&w=majority
MONGODB_URI="mongodb+srv://akmmonir304_db_user:jtY6BpoJjb1VCRGS@cluster0.j2ng4dp.mongodb.net/mobile?retryWrites=true&w=majority&appName=Cluster0"

# How long to wait for the cluster before giving up on one attempt (ms).
DB_TIMEOUT_MS=15000

# Optional. Only used if this PC's own DNS cannot resolve the Atlas SRV record —
# the app then retries the lookup through these resolvers automatically.
DNS_SERVERS=8.8.8.8,1.1.1.1

# ---- auth ----
# Generate one with:  node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=021d063cc7f4118484d8aad3fb2fcb82f02945cb6185c364570b40793712915d96da38ff644b154b61fb7341ed571818
JWT_EXPIRES_IN=30d

# Must match AUTH_BRIDGE_SECRET in frontend/.env.local
OAUTH_SHARED_SECRET=2f4788e31d64d58d0aa5faedf70e1387f2e5449593564b4aaebf05f6efd56d42

# ---- cors ----
# Comma separated. Add your Vercel domain after deploying.
CLIENT_URLS=http://localhost:3000,http://127.0.0.1:3000
