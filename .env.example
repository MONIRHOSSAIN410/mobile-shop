# ---- server ----
NODE_ENV=development
PORT=5000

# ---- database ----
# Local:  mongodb://127.0.0.1:27017/mobile-shop
# Atlas:  mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/mobile-shop?retryWrites=true&w=majority
MONGODB_URI="mongodb://127.0.0.1:27017/mobile-shop"

# ---- auth ----
# Generate one with:  node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=replace-me-with-a-long-random-string
JWT_EXPIRES_IN=30d

# ---- cors ----
# Comma separated. Add your Vercel domain after deploying.
CLIENT_URLS=http://localhost:3000,https://your-frontend.vercel.app
